apply plugin: 'com.android.application'

android {
    signingConfigs {
        release {
            keyAlias 'alibiKey2'
            keyPassword 'Snk85391'
            storeFile file('C:/Users/Senna/Keystore/alibiKey2.jks')
            storePassword 'Snk85391'
        }
    }
    compileSdkVersion 20
    buildToolsVersion "20.0.0"
    defaultConfig {
        applicationId "poorskeletongames.alibi_project"
        minSdkVersion 14
        targetSdkVersion 20
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.release
    }
    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
        debug {
            signingConfig signingConfigs.release
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.google.android.gms:play-services:6.1.11'
    compile project(':BaseGameUtils')
}
